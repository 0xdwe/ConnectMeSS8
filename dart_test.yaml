# dart_test.yaml
#
# Tag emulator-backed tests so the default `flutter test` sweep stays
# fast and offline. The `emulator` tag is opt-in: the default invocation
# excludes it, so the 289-passing Pass 3 baseline is preserved.
#
# Canonical run for emulator-backed Dart tests:
#
#     firebase emulators:exec --only firestore,auth \
#       --project connect-me-rules-test \
#       "flutter test --tags emulator"
#
# See test/firebase_test_setup.dart for details.

tags:
  emulator: {}

exclude_tags: "emulator"
