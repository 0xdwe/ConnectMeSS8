# Plus sheet: replace FAB-menu with AppBar action + bottom sheet

Labels: enhancement, ready-for-agent, components

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Removes the floating 94×94 action button and its overlay-pill menu. Replaces with a small `+` icon in the shell `AppBar` that opens a bottom sheet listing three actions.

**State**: ready-for-agent. Anatomy specified in `DESIGN.md → Components → + Sheet`.

**Codebase notes**:
- `lib/src/features/shell_screen.dart` defines the FAB (`Positioned(top: -35, child: InkWell ...)`) and the action menu (`if (actionsOpen) Positioned.fill...`).
- Existing actions: `Update Connection` and `Add Connection`. We add `Plan Event`.
- Existing `_ActionPill` widgets are 430-wide, 72-tall, w900 weight. Whole pattern goes.
- Existing key `'plus-action-button'` is used by widget tests — preserve it on the new `IconButton` to avoid breaking tests, or update the tests.
- The PRD and DESIGN.md call for the `+` to be top-right of the shell `AppBar`. The shell currently has a custom `AppHeader` (not a Material `AppBar`); this PR introduces an `AppBar` for the shell as part of the migration.

**Implementation notes for the agent**:
- New widget `lib/src/features/modals/plus_sheet.dart` exporting `Future<void> showPlusSheet(BuildContext context)`.
- Sheet uses `showModalBottomSheet`, `radius-lg` top corners. Three rows, each 56pt tall, full-width tappable:
  - **Add Connection** — leading icon `Icons.person_add_alt_1_outlined` in `tokens.inkMuted`. Tap closes sheet, opens `AddConnectionModal` (existing).
  - **Update Connection** — leading icon `Icons.auto_awesome` in `tokens.primary` on `tokens.primaryTint`. Caption underneath: "Paste a chat, AI will categorize." Tap closes sheet, opens existing `UpdatePersonPickerModal` (which then leads to AI Update — issue 018 will rework that flow).
  - **Plan Event** — leading icon `Icons.event_outlined`. Tap closes sheet, opens existing `AddEventModal`.
- Update `shell_screen.dart`:
  - Wrap body in `Scaffold(appBar: AppBar(...), body: ..., bottomNavigationBar: ...)`. The `AppBar` shows app title (`"Connect Me"`) + actions: `IconButton(icon: Icon(Icons.add), key: const Key('plus-action-button'), onPressed: () => showPlusSheet(context))` and `IconButton(icon: avatar, onPressed: () => context.push('/me'))`.
  - Remove the custom `AppHeader` from the shell body — it's replaced by the `AppBar`. The standalone `AppHeader` widget can stay in `crm_widgets.dart` for now (deletion is housekeeping for a later PR).
  - Remove the `Positioned(top: -35, ...)` FAB and the `if (actionsOpen)` overlay branch entirely.
  - `_BottomNav` becomes a clean strip without the gap (`SizedBox(width: 86)` for the FAB cutout) — three or four items at equal width. **Do not change the tab count in this PR**; keep the existing 4 tabs (Home/People/Planner/Settings). Issue 016 collapses to 3 tabs.
  - The state field `actionsOpen` and the overlay code go.
- Tests: keep `Key('plus-action-button')` on the new `IconButton`. Existing widget test that taps it should now find a sheet instead of a floating menu. Update assertions accordingly. Add a test that the sheet shows all three actions.
- Behavior: tapping outside the sheet or the system back gesture dismisses. Drag-down dismissal is native to `showModalBottomSheet`.

## What to build

A clean `+` action in the shell `AppBar` that opens a small bottom sheet with three relationship-shaped actions. The 94pt floating teal button is gone.

## Acceptance criteria

- [ ] `lib/src/features/modals/plus_sheet.dart` exists and exports `showPlusSheet(BuildContext)`.
- [ ] Sheet shows three labeled actions: Add Connection, Update Connection, Plan Event.
- [ ] Update Connection action has a caption "Paste a chat, AI will categorize." and a primary-tinted ✨ icon.
- [ ] Tapping each action closes the sheet and opens the respective existing modal.
- [ ] Shell `body` is wrapped in `Scaffold(appBar: AppBar(...))`. The `AppBar` has a `+` icon (`Key('plus-action-button')`) and an avatar tap (route to `/me`).
- [ ] The `Positioned` FAB and `if (actionsOpen)` overlay are removed from `shell_screen.dart`.
- [ ] `_BottomNav` no longer reserves a gap for the FAB cutout. Remains 4 items (until issue 016).
- [ ] Existing widget tests pass after assertion updates. New test: `showPlusSheet` shows three rows.

## Blocked by

- 009 (design tokens): needs `tokens.primary`, `tokens.primaryTint`, `tokens.surface`.
- 010 (typography): row labels and caption use type tokens.
