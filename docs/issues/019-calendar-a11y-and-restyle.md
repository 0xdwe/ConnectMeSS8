# Calendar a11y + restyle

Labels: enhancement, ready-for-agent, surfaces, accessibility

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Fixes the calendar grid in the Plan tab to meet WCAG AA touch targets, restyles per `DESIGN.md`.

**State**: ready-for-agent. Decisions pinned in `DESIGN.md → Components → Plan tab — calendar`.

**Codebase notes**:
- `lib/src/features/tabs/planner_tab.dart` `_CalendarGrid`: `GridView.builder` with `crossAxisCount: 7`, `childAspectRatio: 0.95`, each cell wraps in `Container(margin: EdgeInsets.all(6))`. At iPhone SE width (~375 - 52 padding) / 7 = ~46 per cell, minus 6+6 margins = ~34 effective tap area. Fails WCAG AA (44pt min).
- Uses `AppTheme.moss` for the selected day fill (becomes `tokens.primary`).
- Today indicator: doesn't exist visually — only the *selected* day gets a fill. Today is missing.
- Event indicator: a single 3pt circle. No multi-event dots.

**Implementation notes for the agent**:
- Replace `Container(margin: EdgeInsets.all(6))` with `InkWell(borderRadius: BorderRadius.circular(14))` directly in the grid cell. The hit area becomes the full grid cell (~46×46 at iPhone SE).
- For very narrow viewports, use a `LayoutBuilder` to enforce `if (cellWidth < 44) cellWidth = 44` and let the grid scroll horizontally. Or use a fixed `mainAxisExtent: 44` via `SliverGridDelegateWithMaxCrossAxisExtent` style to guarantee touch target. Pick the simpler path that meets the constraint.
- Today indicator: a filled circle in `tokens.primary` behind the day number on the day matching `DateUtils.isSameDay(day, DateTime.now())`. Day number renders in `tokens.primaryOn` (white) on top.
- Selected day (when not today): `tokens.primaryTint` background + 2px `tokens.primary` ring. Day number in `tokens.ink`.
- Event indicator: render up to 3 small dots (4pt) under the day number. Use `tokens.primary` for default. (Future: use category color of the most-frequent event for that day. Out of scope.)
- Day-of-week header row: `tokens.inkMuted`, `AppTypography.caption()`.
- Month header arrows: `IconButton`s with 32pt touch targets. Already large enough; just verify.
- Update Plan tab name in the bottom nav (issue 016) and any visible "Planner" string. The file/class name `PlannerTab` can stay.
- Tests:
  - Use `Finder` + `tester.getRect(...)` to assert calendar cell hit area is ≥ 44×44.
  - Test today indicator visible on today.
  - Test selected-day visual differs from today indicator (different colors).
  - Test multi-event day shows up to 3 dots.

## What to build

A calendar that meets WCAG AA touch targets and renders today / selected / events distinctly via tokens.

## Acceptance criteria

- [ ] Calendar day cells are ≥ 44×44 effective touch target on all supported widths (iPhone SE 375 minimum).
- [ ] Today is rendered as a filled `tokens.primary` circle with `tokens.primaryOn` text.
- [ ] Selected day (when not today) shows `tokens.primaryTint` background + 2px `tokens.primary` ring.
- [ ] Days with events show up to 3 small dots in `tokens.primary` under the day number.
- [ ] Day-of-week header uses `tokens.inkMuted` and `AppTypography.caption()`.
- [ ] No raw `AppTheme.moss`, `Colors.black`, `0xFF…` reads in `_CalendarGrid` after migration.
- [ ] Existing widget tests pass after assertion updates. New tests for touch target, today indicator, selected vs today distinct, multi-dot events.

## Blocked by

- 009 (tokens), 010 (typography). Recommend running after 016 so the Plan tab label is settled.
