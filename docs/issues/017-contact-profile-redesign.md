# Contact profile redesign: cut redundant cards

Labels: enhancement, ready-for-agent, surfaces

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Simplifies the contact profile screen. Removes `_BondScorePanel`, `RecommendedActionCard`, and the yellow `InsightCard`. Replaces the teal-block header with a clean header on `surfaceRaised`. Lean profile layout: header + insight summary + relationship facts + history.

**State**: ready-for-agent. Layout pinned in `DESIGN.md`. Removal list pinned in `PRODUCT.md` and `DESIGN.md → Removed`.

**Codebase notes**:
- `lib/src/features/contact_profile_screen.dart` currently stacks (top-to-bottom): teal header (avatar/name/email/edit), Update-with-AI button, `_BondScorePanel` + `RecommendedActionCard` (side by side or stacked), `InsightCard`, `RelationshipFactsCard`, `CommunicationChannelsCard`, `InteractionFrequencyCard`, history.
- `_BondScorePanel` is the inline teal block with the `48`-pt score and "Strong connection!" hardcoded subtitle.
- `RecommendedActionCard` is the orange "You can gain X% Connection Score" card.
- `InsightCard` is the yellow-bordered `#FFE45C` "AI Insight" expandable card.
- `RelationshipFactsCard` shows "Relationship: Family / Known Since: 27 years / Last contact: 2026-04-21".
- `CommunicationChannelsCard` and `InteractionFrequencyCard` show channels (chip row) and frequency (12-bar chart).
- Issue 013 already removed the teal block at the top — this PR builds on that.

**Implementation notes for the agent**:
- New layout for `ContactProfileScreen`:
  1. `Scaffold` `AppBar` with a "Edit" action (replaces the current `IconButton.filledTonal`).
  2. **Header section** on `tokens.surfaceRaised`:
     - Avatar (size 96) wrapped by `BondRing(connection: person, size: 96)`.
     - Name (`AppTypography.display()`).
     - Category dot + label (small `AppTypography.caption()`).
     - One subtle line: `insight.summary` (`AppTypography.body()`, `tokens.inkMuted`). No yellow border, no expand-on-tap behavior.
  3. **Primary action**: full-width `FilledButton.icon` "Update with AI" (already exists, keep). Style with `tokens.primary`.
  4. **Relationship facts** card:
     - Two-column layout: "Relationship" / "Known since N years".
     - Below: "Last contact: <date>" line.
     - Use the existing `RelationshipFactsCard` mostly as-is, restyled with tokens.
  5. **History list**:
     - `SectionTitle("History")` if any.
     - Each `CrmInteraction` rendered as a slim card: icon, title, date + note.
     - Empty case: warm copy ("Sarah's new — you'll fill this in over time.") in `inkMuted`.
- **Remove**:
  - `_BondScorePanel` widget (delete the inline class).
  - `RecommendedActionCard` invocation. The widget can stay in `crm_widgets.dart` for now if other screens use it; verify with `grep -rn 'RecommendedActionCard'` and delete if unused.
  - `InsightCard` invocation (the yellow expandable). The summary text is now in the header. Drop `insight.why` for this PR — it can come back as a tap-to-expand on the header line in a future PR if needed.
  - `CommunicationChannelsCard` and `InteractionFrequencyCard` invocations on this screen. Delete from this screen only — leave the widgets in `crm_widgets.dart` (deletion is housekeeping). Their data is still computed in `contactInsightFor`; that's fine.
- The `LayoutBuilder` two-column branch goes (no narrow vs wide split needed; the layout is single-column).
- `firstWhere((c) => c.id == contactId)` should add `orElse: () => null` and route back if null. Cross-cutting; issue 023 will sweep all of these, but do this one inline since you're already touching the file.
- Tests: update assertions on the existing profile screen layout. Add new tests:
  - Profile screen renders without `_BondScorePanel` literal "Strong connection!" or `RecommendedActionCard`.
  - `BondRing` is present at size 96 in the header.
  - History section absent when `interactions` is empty for the contact; warm empty copy visible.

## What to build

A calm contact profile screen. Bond ring + insight summary + facts + history. No grading, no orange CTAs, no yellow warning borders.

## Acceptance criteria

- [ ] `_BondScorePanel` class deleted from `contact_profile_screen.dart`.
- [ ] `RecommendedActionCard`, `InsightCard`, `CommunicationChannelsCard`, `InteractionFrequencyCard` are no longer invoked on `ContactProfileScreen`.
- [ ] Header section uses `BondRing(size: 96)` and renders name + category + summary line.
- [ ] "Update with AI" full-width primary button visible.
- [ ] Relationship facts card visible with two-column layout.
- [ ] History list renders interactions; warm empty copy when none.
- [ ] No literal text "Strong connection!" or "Recommended Action!" appears on the screen.
- [ ] `firstWhere` for contact id has `orElse` and gracefully handles missing contact.
- [ ] Tests pass with assertions updated. New tests for layout and empty-history copy.

## Blocked by

- 009 (tokens), 010 (typography), 012 (BondRing), 013 (TealPageHeader removed from this screen).
