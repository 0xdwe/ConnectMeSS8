# Remove user-level XP, levels, and points from data model

Labels: enhancement, ready-for-agent, foundation

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Removes self-gamification from the data model. Per-friend `bondScore` is unaffected — only the user's `totalPoints`, `currentLevel`, `nextLevelPoints` go.

**State**: ready-for-agent. Behavior is fully specified in `PRODUCT.md` ("Removed entirely: user-level XP/points/levels"). Mechanical removal.

**Codebase notes**:
- `lib/src/models/social_models.dart`: `AppUser` declares `totalPoints`, `currentLevel`, `nextLevelPoints` as `final int`. `copyWith` plumbs them through.
- `lib/src/state/app_state.dart`: `AppState.seeded()` constructs `AppUser(... totalPoints: 240, currentLevel: 7, nextLevelPoints: 300)`.
- `AppController.signUp` and `updateUser` accept the existing `AppUser.copyWith` shape.
- No UI surface (after audit) reads these — `home_tab.dart`, `profile_screen.dart`, `contact_profile_screen.dart`, `tabs/settings_tab.dart` do not currently render them. Confirm with `grep -rn 'totalPoints\|currentLevel\|nextLevelPoints' lib/`.
- `test/state/app_state_test.dart` and `test/widget_test.dart` may reference the values in `AppUser` constructors.

**Implementation notes for the agent**:
- Delete `totalPoints`, `currentLevel`, `nextLevelPoints` from `AppUser` (constructor, fields, `copyWith`).
- Update the seeded `AppUser` in `AppState.seeded()`.
- Update `AppController.signUp(...)` and `updateUser(...)` if their bodies reference removed fields (they should not).
- Update any test fixtures that construct `AppUser` directly. `grep -rn 'AppUser(' test/` to find them.
- Per-friend `bondScore` (on `Connection`) and `averageConnectionScore` (on `AppState`) STAY. They are relationship-state visualizations, not user gamification, per `PRODUCT.md` principle 5.
- Run `flutter analyze` and `flutter test`. Both must pass.

## What to build

A clean removal of user-level gamification fields. No replacement, no migration logic, no settings affordance — they simply do not exist.

## Acceptance criteria

- [ ] `AppUser` no longer declares `totalPoints`, `currentLevel`, or `nextLevelPoints`.
- [ ] `AppUser.copyWith` no longer plumbs them.
- [ ] `AppState.seeded()` no longer constructs them.
- [ ] `grep -rn 'totalPoints\|currentLevel\|nextLevelPoints' lib/ test/` returns zero results.
- [ ] `flutter analyze` returns zero errors and zero new warnings.
- [ ] All existing tests pass.
- [ ] Per-friend `bondScore` and `averageConnectionScore` are unchanged.

## Blocked by

None - can start immediately, in parallel with 009 and 010
