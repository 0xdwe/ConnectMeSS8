# Recommendation card rewrite: conversational, no priority pills

Labels: enhancement, ready-for-agent, surfaces

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Rewrites the recommendation card. Removes priority pills, warning icons, the highlighted-card border at index 1, and the 💬 quote-bubble. Adopts conversational second-person copy.

**State**: ready-for-agent. Layout and copy guidance specified in `DESIGN.md → Components → Recommendation card (Home)` and `PRODUCT.md → Anti-references` (no priority text).

**Codebase notes**:
- Card defined in `lib/src/widgets/crm_widgets.dart` (`RecommendationCard`). Renders avatar, name, warning icon + reason, quote-bubble with insight, two chips (category + priority), score ring, chevron. Plus an optional moss border highlight when `highlight: true`.
- Used in `lib/src/features/tabs/home_tab.dart` (with `highlight: i == 1`) and `lib/src/features/recommendations_screen.dart` (verify).
- `Recommendation.priority` is currently rendered as a colored chip with text "high priority" / "medium priority" / "low priority".
- Seeded recommendations in `app_state.dart` use the same priority strings.

**Implementation notes for the agent**:
- Rewrite `RecommendationCard` from scratch. New layout (vertical):
  - **Row 1**: `BondRing` (size 56, from issue 012) + `Connection.name` (`AppTypography.h2()`) + small category dot (8pt, color from category palette in `DESIGN.md`).
  - **Row 2 (headline)**: `recommendation.reason` repurposed as conversational copy (`AppTypography.bodyLg()`). The seeded strings are too CRM-like; transform at render time:
    - "Over 1 month since last contact" → "Mike's been quiet for a while."
    - "3 weeks without interaction" → "Jessica is starting to drift."
    - "Great momentum - keep it going!" → "You and Emily have been close lately."
  - Or, simpler: change the seeded `Recommendation` strings directly to the conversational versions and leave the card as a thin renderer. Choose this path for less surface area.
  - **Row 3 (insight)**: `recommendation.insight` rendered as a quiet line (`AppTypography.body()`, `tokens.inkMuted`). No quote bubble, no italic, no 💬 emoji. Just the sentence.
  - **Row 4 (actions)**: two buttons. Primary `FilledButton('Update Connection')` (filled `tokens.primary`) and secondary `TextButton('Open profile')`. Both at full width split 50/50, or stacked on narrow screens.
- Remove from card:
  - `priority` chip and any reference to `recommendation.priority` in render code.
  - `highlight: bool` parameter and the moss-bordered highlighted state.
  - The warning `Icon(Icons.error_outline)` next to the reason.
  - The 💬 quote bubble container.
  - The right-side `ScoreRing` + "Score" label column (the bond ring is already in Row 1).
  - The trailing chevron (the row IS the action; the chevron is redundant when there's a "Open profile" button).
- Update `Recommendation` model: keep `priority: String` for now (it can sort cards internally — most-overdue first), but UI code never reads it. A follow-up PR can convert to an enum.
- Update `home_tab.dart`: remove `highlight: i == 1`. Use `BondRing` semantics for ordering — sort recommendations by `priority` internally if needed.
- Update seeded recommendations in `AppState.seeded()` and the static `recommendations` getter to use conversational `reason` strings.
- Tests: update any widget test asserting on "high priority" text or warning icon presence. New tests: card renders no "priority" text; both action buttons present and tappable.

## What to build

A friendlier, lower-density recommendation card that respects "never shame the user." The page hero of the Home tab.

## Acceptance criteria

- [ ] `RecommendationCard` rewritten per `DESIGN.md → Components → Recommendation card`.
- [ ] No reference to `recommendation.priority` in render code; `grep -n 'priority' lib/src/widgets/crm_widgets.dart` returns zero.
- [ ] No 💬 emoji, no `Icons.error_outline`, no quote-bubble container, no `highlight` parameter.
- [ ] Card has primary action "Update Connection" (filled, `tokens.primary`) and secondary action "Open profile" (text button).
- [ ] Seeded recommendations use conversational reason strings ("Mike's been quiet for a while," etc.).
- [ ] `home_tab.dart` no longer passes `highlight: i == 1`.
- [ ] All existing tests pass after assertion updates. New tests cover the absence of "priority" text and the presence of both action buttons.

## Blocked by

- 009 (tokens), 010 (typography), 012 (BondRing).
