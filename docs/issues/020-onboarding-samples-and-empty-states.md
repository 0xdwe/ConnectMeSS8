# Onboarding: sample tagging + warm empty states

Labels: enhancement, ready-for-agent, polish

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Tags seeded "sample" friends visibly on first run, adds a sweep-action to remove them, and replaces functional empty states with warm copy.

**State**: ready-for-agent. Decisions pinned in `PRODUCT.md → Strategic decisions captured` and `DESIGN.md → Components → Empty state`.

**Codebase notes**:
- `AppState.seeded()` creates 5 contacts. None are flagged as samples.
- Empty states currently rely on a generic `EmptyState` widget in `crm_widgets.dart` rendering "Nothing yet." with an inbox icon. Used in only a few places; many tabs render nothing instead of an empty state.
- Per `PRODUCT.md`: "Empty states: warm and resting. Absence is reframed as a positive when possible."

**Implementation notes for the agent**:
- Model:
  - Add `final bool isSample;` to `Connection`. Default `false`. Update `copyWith`. Update tests.
  - Set `isSample: true` on the 5 seeded contacts in `AppState.seeded()`.
- UI:
  - In the People tab list row (`ContactListCard`), if `connection.isSample`, show a small `Sample` tag (text or chip in `tokens.inkSubtle`, `AppTypography.caption()`) beside the name.
  - In Settings (the new `/settings` route from issue 016), add a row at the bottom of the Account section: "Remove sample friends." Tap → `AppController.removeSampleConnections()` clears any `Connection` where `isSample == true`. Confirmation dialog: "Remove the 5 sample friends and their interactions?" with primary action red (`tokens.danger`).
  - Wire up `AppController.removeSampleConnections()` to filter out sample contacts plus their interactions and events.
- Empty states (one warm sentence each, no illustrations):
  - **Home, no recommendations**: "You're in touch with everyone right now." (centered `bodyLg`, `tokens.inkMuted`)
  - **People tab, no contacts**: "No connections yet. Tap + to add someone." (centered `bodyLg`, `tokens.inkMuted`)
  - **People tab, search yields nothing**: "Nothing matches '<query>'. Try a different word." (`bodyLg`, `tokens.inkMuted`)
  - **Plan tab, no events**: "Quiet week ahead." (`bodyLg`, `tokens.inkMuted`)
  - **Contact profile, no history**: "<Name> is new — you'll fill this in over time." (`body`, `tokens.inkMuted`)
  - **AI preview, parsing failed**: "I'm not sure what to do with this. Try adding a name or a date." (`body`, `tokens.inkMuted`) — applies to issue 018 if not already wired.
- The existing `EmptyState` widget can be repurposed as a thin wrapper, or replaced with inline `Center(child: Padding(child: Text(...)))` blocks per screen. Prefer the inline approach so each screen owns its copy.
- Tests:
  - New unit test: `removeSampleConnections` clears only sample contacts + their interactions + events; non-sample contacts untouched.
  - New widget test: People tab with zero contacts shows the warm empty copy.
  - New widget test: Home tab with zero recommendations shows the warm copy.
  - Update existing tests if any assert on "Nothing yet." or `EmptyState` literal copy.

## What to build

Onboarding that doesn't bait-and-switch (samples are visibly samples), empty states that read like rest, not absence.

## Acceptance criteria

- [ ] `Connection.isSample: bool` exists, defaults `false`, plumbed through `copyWith`.
- [ ] Seeded contacts in `AppState.seeded()` have `isSample: true`.
- [ ] `ContactListCard` shows a "Sample" tag for sample contacts.
- [ ] Settings has a "Remove sample friends" row with confirmation dialog.
- [ ] `AppController.removeSampleConnections()` clears samples + their interactions + events.
- [ ] Home, People, Plan, contact-history, AI-preview-failed empty states use the warm copy specified above.
- [ ] No `EmptyState` widget with "Nothing yet." copy is rendered anywhere.
- [ ] All existing tests pass. New unit + widget tests added.

## Blocked by

- 009 (tokens), 010 (typography), 015 (recommendation card / Home), 016 (/settings route), 017 (contact profile history empty state), 018 (AI preview failed copy).
