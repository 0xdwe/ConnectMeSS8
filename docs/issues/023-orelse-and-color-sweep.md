# Cross-cutting: orElse on firstWhere + hex/black colors sweep

Labels: enhancement, ready-for-agent, cross-cutting

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement / cleanup. Sweeps two systemic issues identified in the audit: `firstWhere` without `orElse` (crashes after stale-id navigation) and stray `Colors.black54/38` reads (bypasses tokens).

**State**: ready-for-agent. Mechanical sweep, run after the surface-redesign waves so we're not chasing churn.

**Codebase notes**:
- `firstWhere((...))` without `orElse` appears in:
  - `lib/src/features/tabs/home_tab.dart` (recommendation contact lookup)
  - `lib/src/features/contact_profile_screen.dart` (`person = state.connections.firstWhere(...)`)
  - `lib/src/features/ai_update_screen.dart` (`person = ref.watch(...).connections.firstWhere(...)`)
  - `lib/src/features/recommendations_screen.dart` (verify)
- `Colors.black54`, `Colors.black38`, raw `Colors.black` for text appear across feature widgets despite `inkMuted` / `inkSubtle` / `ink` tokens existing after issue 009.

**Implementation notes for the agent**:
- For each `firstWhere`:
  - Add `orElse: () => null` (and update the local variable type to nullable) — OR — use a helper extension `connections.byId(id) -> Connection?`.
  - If the lookup returns `null`, route back (`Navigator.of(context).pop()` or `context.go('/app')`) with a brief `SnackBar`: "Contact removed."
  - Two callers in `home_tab.dart` are the most common stale-id source. Make sure `RecommendationCard` consumers tolerate a missing contact gracefully.
- For each `Colors.black54/38/black` in widget code (excluding `Material` defaults set in `app_theme.dart`):
  - Replace with `tokens.inkMuted` / `tokens.inkSubtle` / `tokens.ink` per visual context.
  - Run `grep -rn 'Colors.black5\|Colors.black3\|Colors.black,' lib/src` to verify.
- Same for the leftover hex reads — `0xFF667085`, `0xFFE0F0F0`, `0xFFEFFBF8`, `0xFFE5E7EB`, etc. After issue 009 most are gone; the long-tail offenders this sweep will catch are inside widgets that survived earlier waves untouched.
- Tests:
  - Add a test for the stale-id case: navigate to `/contact/<bad-id>` → expect `SnackBar` and routed back to shell.
  - Add a static lint-style test (or an integration assertion) that runs `grep` over `lib/src` and asserts zero hits for `Colors.black5`, `Colors.black3`, `0xFF` outside `app_tokens.dart` and `app_theme.dart`.

## What to build

Defensive lookup helpers + final color sweep so the codebase is internally consistent before we ship.

## Acceptance criteria

- [ ] No `firstWhere(...)` without `orElse` in `lib/src/features/`.
- [ ] Stale `/contact/<bad-id>` routes pop back with a "Contact removed" snackbar instead of crashing.
- [ ] `grep -rn 'Colors.black5\|Colors.black3\|Colors.black,' lib/src/features lib/src/widgets` returns zero results.
- [ ] `grep -rn '0xFF' lib/src/features lib/src/widgets` returns zero results (allowed only in `app_tokens.dart` and `app_theme.dart`).
- [ ] All tests pass; new test covers the stale-id route case.

## Blocked by

- 009 (tokens must exist for the color sweep).
- Recommend running last: 020 onboarding has likely touched the same files.
