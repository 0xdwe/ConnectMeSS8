## Parent

`docs/prd/2026-05-15-update-with-ai-single-flow-prd.md`

Labels: enhancement, ready-for-agent

> *This was generated by AI during triage.*

## What to build

Remove the Share Activity surface and its entry points so Update with AI is the only update flow. Update Plus menu and the Personal Connection Dashboard so Share Activity no longer appears. Remove the Share Activity modal file. Update or remove tests that referenced Share Activity.

## Acceptance criteria

- [ ] Plus menu shows only "Update Connection" and "Add Connection".
- [ ] Personal Connection Dashboard no longer shows a Share Activity icon button or full-width Share Activity button.
- [ ] No code path opens the Share Activity modal.
- [ ] `lib/src/features/modals/shared_activity_modal.dart` is deleted.
- [ ] Tests previously named "contact screen can share activity note" and "plus menu add connection mutates visible people list" stay valid; the share activity test is removed.
- [ ] Existing Update Connection picker still routes to `/ai-update/:id`.
- [ ] All tests pass.

## AI Triage Brief

**Category**: enhancement.

**State**: ready-for-agent.

**Codebase notes**:
- Plus menu lives in `lib/src/features/shell_screen.dart`.
- Contact profile lives in `lib/src/features/contact_profile_screen.dart`.
- Share activity modal: `lib/src/features/modals/shared_activity_modal.dart`.
- Existing share-activity widget test: `test/widget_test.dart` (`contact screen can share activity note`).

**Implementation notes for the agent**:
- Drop the "Share Activity" `_ActionPill` from `_ShellScreenState`.
- In `ContactProfileScreen` remove both Share Activity entry points (icon row + full-width button).
- Delete `shared_activity_modal.dart` and remove imports.
- Delete the share-activity widget test; do not replace it in this slice (next slice rewires Update with AI tests).

## Blocked by

None - can start immediately
