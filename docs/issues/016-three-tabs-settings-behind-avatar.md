# Shell IA: 3 tabs, Settings behind avatar

Labels: enhancement, ready-for-agent, surfaces

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Reduces bottom nav from 4 tabs (Home/People/Planner/Settings) to 3 (Home/People/Plan). Settings becomes a route reachable via the avatar button on the shell `AppBar`.

**State**: ready-for-agent. Decision pinned in `PRODUCT.md → Strategic decisions captured`.

**Codebase notes**:
- Tab order in `lib/src/features/shell_screen.dart`: `[HomeTab(), PeopleTab(), PlannerTab(), SettingsTab()]` indexed `0..3`. `_NavItem` for each.
- `AppState.selectedTab: int` and `AppController.setTab(int)` drive selection.
- `lib/src/features/tabs/settings_tab.dart` exports `SettingsTab` widget. Reachable as a tab.
- Shell `AppBar` (added in issue 014) has an avatar `IconButton` going to `/me`. We change it to `/settings` (or repurpose `/me` as the settings + profile screen).
- `lib/src/features/profile_screen.dart` exists as `/me`. It contains user-editing flows already.
- Plan tab name in current code: "Planner" (label and text). PRD locks "Plan."

**Implementation notes for the agent**:
- In `shell_screen.dart` `_tabs`: remove `SettingsTab()`. Final list: `[HomeTab(), PeopleTab(), PlannerTab()]`.
- Update `_BottomNav` to render three `_NavItem`s, equally spaced. Remove the `SettingsTab` nav entry.
- Rename the Planner nav label from "Planner" to "Plan" per PRD. Keep the tab class name `PlannerTab` for now (avoid file rename churn — note in PR).
- The shell `AppBar` avatar `IconButton` (added in issue 014): the `onPressed` should now route to a screen that combines Settings with the existing profile content. Two options:
  - **Option A (simpler)**: route to a new `/settings` path that renders `SettingsTab()` (renamed to `SettingsScreen` in a wrapper). Profile editing stays at `/me` if it already exists; add a "View profile" row inside Settings linking to `/me`.
  - **Option B (consolidating)**: merge `ProfileScreen` and `SettingsTab` content under one `/settings` route. More work; not needed this PR.
- **Pick Option A.** Less risk, keeps existing `/me` working.
- Add a new route `/settings` in `connect_me_app.dart` rendering a `SettingsScreen` widget that wraps `SettingsTab` body inside a `Scaffold(appBar: AppBar(title: 'Settings'), body: ...)`.
- Update the avatar `IconButton` in shell `AppBar`: route to `/settings`.
- Update `setTab` to clamp at 0..2 (or change the type to an enum). If existing widget tests pump the app and tap a "Setting" nav item, update them to navigate to `/settings` via the avatar instead.
- `AppState.selectedTab` semantics shift: 0 Home, 1 People, 2 Plan. Existing tests asserting `state.selectedTab == 3` for Settings need updating; settings is no longer a tab.
- Don't delete `SettingsTab` (yet) — it becomes the body of `SettingsScreen`. Or extract its `ListView` into a shared widget.
- Verify `tabs/settings_tab.dart` doesn't reference `selectedTab: 3` anywhere.

## What to build

A 3-tab shell that respects PRODUCT.md's cognitive-load ceiling. Settings is one tap away via the avatar, not a bottom-nav slot.

## Acceptance criteria

- [ ] Shell `_tabs` contains exactly `[HomeTab(), PeopleTab(), PlannerTab()]`.
- [ ] Bottom nav has three items: Home / People / Plan.
- [ ] "Plan" label appears in the bottom nav (not "Planner" or "Setting").
- [ ] New route `/settings` exists and renders the settings content with a plain `AppBar`.
- [ ] Shell `AppBar` avatar `IconButton` routes to `/settings`.
- [ ] `AppState.selectedTab` value is `0`, `1`, or `2`.
- [ ] `flutter analyze` clean.
- [ ] All existing widget tests pass after navigation/assertion updates. New test: tapping the avatar opens the Settings screen.

## Blocked by

- 014 (plus sheet, which introduces the shell `AppBar` with avatar action).
