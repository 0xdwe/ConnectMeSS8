# Remove TealPageHeader from secondary screens

Labels: enhancement, ready-for-agent, components

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Removes the giant teal/purple color block that occupies ~22% of every secondary screen, replacing with plain Material `AppBar`.

**State**: ready-for-agent. Behavior specified in `DESIGN.md → Layout → Page-level structure`.

**Codebase notes**:
- `TealPageHeader` defined in `lib/src/widgets/crm_widgets.dart`.
- Used in: `lib/src/features/ai_update_screen.dart`, `lib/src/features/recommendations_screen.dart` (verify), `lib/src/features/profile_screen.dart` (verify), and the `contact_profile_screen.dart` uses an inline equivalent (`Container(color: AppTheme.moss, ...)`) that should also go.
- The shell screen `_BottomNav` and `AppHeader` are NOT this header — they're separate. Don't touch them in this PR.

**Implementation notes for the agent**:
- For each screen using `TealPageHeader` or an inline teal `Container` header:
  - Replace with a Material `AppBar` (in `Scaffold(appBar: AppBar(...))`) with `title: Text('Page Title', style: AppTypography.h2())`, no shadow (`elevation: 0`), background `tokens.surface`, foreground `tokens.ink`.
  - The "back" affordance comes from `AppBar.leading` / Flutter's automatic back button; remove the custom `_HeaderBackButton`.
  - The subtitle (currently rendered in 23pt white inside the teal block) becomes a small `caption` line under the page hero in the body, or merges into the hero — judge per screen.
- For `ContactProfileScreen` specifically: replace the teal `Container` block. The avatar + bond ring + name + email move into a clean header section on `tokens.surfaceRaised`. Issue 017 owns the full profile redesign — this PR only does the *header swap*; leave the rest of the page as-is until 017.
- Delete `TealPageHeader` from `crm_widgets.dart` after the last consumer is migrated.
- Tests: update any test that asserts on a teal block's existence or on the literal text rendered by `TealPageHeader`. Most tests should not care.

## What to build

Plain Material `AppBar`s on secondary screens. No giant color block before content.

## Acceptance criteria

- [ ] `TealPageHeader` is deleted from `crm_widgets.dart`.
- [ ] `ai_update_screen.dart` uses `Scaffold(appBar: AppBar(title: ...))`.
- [ ] `recommendations_screen.dart` uses `Scaffold(appBar: AppBar(...))` (if applicable).
- [ ] `profile_screen.dart` uses `Scaffold(appBar: AppBar(...))` (if applicable).
- [ ] `contact_profile_screen.dart`'s teal-block header is replaced with a clean header section on `tokens.surfaceRaised`. Avatar + name + email visible. Bond ring location may be temporary (final layout in 017).
- [ ] `_HeaderBackButton` (custom white back arrow + label) is removed; back navigation is provided by `AppBar`'s automatic back button.
- [ ] Existing widget tests still pass after minor updates.
- [ ] Visual smoke check: every secondary screen has a clean, plain header. No drenched color block at the top.

## Blocked by

- 009 (design tokens): needs `tokens.surface`, `tokens.surfaceRaised`, `tokens.ink`.
- 010 (typography): needs `AppTypography.h2()` for `AppBar` titles.
