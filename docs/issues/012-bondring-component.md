# BondRing component + remove BigScoreCircle

Labels: enhancement, ready-for-agent, components

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Replaces the existing `ScoreRing` and `BigScoreCircle` with a single `BondRing` component that visualizes relationship state without grading the user.

**State**: ready-for-agent. Anatomy and behavior specified in `DESIGN.md → Components → Bond ring`.

**Codebase notes**:
- `lib/src/widgets/crm_widgets.dart` defines `ScoreRing` (used in `ContactListCard`, `RecommendationCard`, hero contexts) and `BigScoreCircle` (used in `home_tab.dart` as the page hero).
- `_BondScorePanel` in `contact_profile_screen.dart` is a *third* score display — a teal block with `48`-pt number and hardcoded "Strong connection!" subtitle. Goes away in issue 017; this PR only replaces the ring.
- Tier bands per `DESIGN.md`: close ≥ 80 → `tokens.primary`; steady 50-79 → `tokens.inkMuted`; drifting < 50 → `tokens.secondary`.
- Trend arrow data: not currently in `Connection`. For this PR, add a getter `Connection.bondTrend` returning `BondTrend.up | down | flat` derived from a stub (e.g., `bondScore >= 70 ? BondTrend.up : BondTrend.flat`). Wave 4 will replace with real history-based logic.

**Implementation notes for the agent**:
- New file `lib/src/widgets/bond_ring.dart` exporting:
  - `enum BondTier { close, steady, drifting }` with a `from(int score)` factory.
  - `enum BondTrend { up, down, flat }`.
  - `class BondRing extends StatefulWidget` with constructor `BondRing({required Connection connection, double size = 64})`.
- Anatomy:
  - Stack of: avatar `CircleAvatar` (size: `size`), background ring (full circle, stroke 3, color `tokens.border` at 40% opacity), foreground arc (`bondScore / 100` fraction, stroke 3, color from `BondTier`), and trend arrow (12px Material icon at 4 o'clock outside the ring) when `trend != flat`.
  - Use `CustomPaint` for the arc.
  - Tap: toggles a small caption *below* the ring (e.g., `"73 · close"`). Caption is rendered by the parent if needed; the bond ring itself just emits a tap and shows nothing internally — keep the component focused. Or expose a `showLabel: bool` and let the consumer wrap in a column. Pick the simpler path; document the choice.
- Semantic label: `"<name>, <tier>, <trend>"` for screen readers (e.g., "Sarah, close, trending up"). Set via `Semantics(label: ..., child: ...)`.
- Touch target: when `size < 44`, wrap in a `SizedBox(width: 44, height: 44)` centered around the ring so the hit area meets WCAG.
- Replace `ScoreRing` in `ContactListCard` and `RecommendationCard` with `BondRing`. The `score` parameter becomes the full `Connection`.
- Delete `BigScoreCircle`. Update `home_tab.dart`: remove the hero `BigScoreCircle(score: state.averageConnectionScore)`. Replace with a quiet `bodyLg` greeting line ("Hi, Alex.") sized for the new design — the page hero is no longer a score, it's a name. Issue 015 (recommendation card rewrite) will rebuild the rest of Home.
- Delete `BigScoreCircle` references in tests; update or remove related assertions.
- DO NOT add the fill animation in this PR. That's issue 021. The arc renders in its final state, no transitions.
- DO NOT change the contact profile screen layout. Issue 017 owns that.

## What to build

A `BondRing` that shows relationship state visually, with a tier color, an optional trend arrow, accessible semantics, and a 44pt minimum touch target. Replaces three different score-display widgets with one.

## Acceptance criteria

- [ ] `lib/src/widgets/bond_ring.dart` exports `BondRing`, `BondTier`, `BondTrend`.
- [ ] Tier color mapping matches `DESIGN.md` (close → primary, steady → inkMuted, drifting → secondary).
- [ ] Trend arrow appears at 4 o'clock when `trend != flat`. Hidden otherwise.
- [ ] Tap on ring emits a callback; consumer can render a "73 · close" caption.
- [ ] Touch target ≥ 44×44 regardless of `size`.
- [ ] Semantic label format: "Sarah, close, trending up".
- [ ] `BigScoreCircle` is removed from `crm_widgets.dart`.
- [ ] `ScoreRing` is removed from `crm_widgets.dart` (or marked `@Deprecated` if any caller can't migrate this PR — note in PR description).
- [ ] `home_tab.dart` no longer renders `BigScoreCircle`.
- [ ] `ContactListCard` and `RecommendationCard` use `BondRing`.
- [ ] All existing widget tests pass after updates. Specifically tests that rely on `ScoreRing` rendering a number need updates.
- [ ] New widget test: `BondRing` with score 90 renders with primary color; score 60 with inkMuted; score 30 with secondary. Tap callback fires.

## Blocked by

- 009 (design tokens): needs `tokens.primary`, `tokens.secondary`, `tokens.inkMuted`, `tokens.border`.
- 010 (typography): not strictly required, but the caption uses `caption` token.
