## Parent

`docs/prd/2026-05-15-tappable-recommendation-cards-prd.md`

Labels: enhancement, ready-for-agent

> *This was generated by AI during triage.*

## What to build

Wire the Outreach Recommendations screen so its recommendation cards open the corresponding contact's Personal Connection Dashboard when tapped. Reuses the tappable recommendation card behavior added in issue 004.

## Acceptance criteria

- [ ] Recommendation cards on the Outreach Recommendations screen are tappable.
- [ ] Tapping a card on that screen opens the contact's Personal Connection Dashboard.
- [ ] Card visual styling stays consistent with the Home tab (chevron present).
- [ ] Existing widget tests still pass.
- [ ] New widget test covers tapping a recommendation from the Outreach Recommendations screen and arriving at the dashboard.

## AI Triage Brief

**Category**: enhancement. Polish hookup that mirrors issue 004 in the second surface.

**State**: ready-for-agent. Concrete and small.

**Codebase notes**:
- The Outreach Recommendations screen is `lib/src/features/recommendations_screen.dart`. It already routes from the Home tab "View All ->" link.
- After issue 004 lands, `RecommendationCard` accepts `onTap`. Pass the same callback shape here.
- The screen is reachable via the route `/recommendations`.

**Implementation notes for the agent**:
- In `RecommendationsScreen`, pass `onTap: () => context.push('/contact/${recommendation.contactId}')` to each card.
- Avoid duplicating any helper logic added in issue 004; reuse the same card module.
- Do not introduce new state in the recommendations screen beyond what is needed to route the tap.

## Blocked by

- docs/issues/004-tappable-recommendation-card-from-home.md
