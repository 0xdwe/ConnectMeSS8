# Sign up with mock account details

Labels: enhancement, ready-for-agent

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Extends the auth screen with a Sign up mode.

**State**: ready-for-agent. Builds on issue 001 with concrete acceptance criteria.

**Codebase notes**:
- After 001 lands, the auth screen will own a Login form. Add a mode toggle to also show Sign up.
- `AppController.updateUser(...)` already supports updating name/email; a focused `signUp(name, email)` helper that updates the user and calls `signIn()` keeps locality at the session seam.
- The session user is rendered in the app header (`lib/src/features/shell_screen.dart`) and on the profile page.

**Implementation notes for the agent**:
- Add Sign up fields: full name, email, password, confirm password.
- Validation: required fields, email contains `@`, password length ≥ 6, password matches confirm.
- On valid submit, update the session user from the form values, then sign in and route to `/app`.
- Tests: valid signup updates the displayed name/email and enters the app; mismatched passwords show validation.

## What to build

Build the signup path for the mock ConnectMe auth flow. A user switches to Sign up, enters full name, email, password, and confirm password, submits valid details, and lands in the main app with the session profile updated to the signed-up name and email.

## Acceptance criteria

- [ ] Auth screen has a Sign up mode reachable from Login mode.
- [ ] Sign up mode has full name, email, password, and confirm password fields.
- [ ] Empty required fields show clear validation messages.
- [ ] Email must contain `@`.
- [ ] Password must be at least 6 characters.
- [ ] Confirm password must match password.
- [ ] Successful signup updates the session user's displayed name and email.
- [ ] Successful signup navigates to the main app shell.
- [ ] Existing seeded connections/events/recommendations remain unchanged after signup.
- [ ] Widget test covers valid signup entering the app and showing the signed-up user.
- [ ] Widget test covers invalid signup validation, including mismatched passwords.

## Blocked by

- docs/issues/001-login-with-mock-credentials.md
