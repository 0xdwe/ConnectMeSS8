# Login with mock credentials

Labels: enhancement, ready-for-agent

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Adds a real-looking login form to the existing mock auth screen.

**State**: ready-for-agent. Acceptance criteria are concrete; no human judgment needed.

**Codebase notes**:
- Auth lives in `lib/src/features/auth_screen.dart` (currently a single mock `Enter mock workspace` button keyed `sign-in-button`).
- Session state in `lib/src/state/app_state.dart` exposes `signIn()` on `AppController`.
- Router in `lib/src/app/connect_me_app.dart` sends `/auth` → `/app` after sign-in.
- Existing widget tests (`test/widget_test.dart`) tap the `sign-in-button` key — keep that key on the new login submit button or update tests to match.

**Implementation notes for the agent**:
- Replace the mock button with a Login form: email + password fields, validation messages, submit.
- Validation: required, email contains `@`, password length ≥ 6.
- On valid submit call `signIn()` and route to `/app`.
- Add widget tests covering valid login + invalid validation.

## What to build

Build the login path for the mock ConnectMe auth flow. A user enters a valid-looking email and password, submits the form, and lands in the main app without needing a backend account.

## Acceptance criteria

- [ ] Auth screen has a Login mode with email and password fields.
- [ ] Empty email/password show clear validation messages.
- [ ] Email must contain `@`.
- [ ] Password must be at least 6 characters.
- [ ] Any valid email/password logs in successfully.
- [ ] Successful login navigates to the main app shell.
- [ ] Existing seeded user/profile data remains unchanged after login.
- [ ] Widget test covers valid login entering the app.
- [ ] Widget test covers invalid login validation.

## Blocked by

None - can start immediately
