# Polish auth mode switching and prototype messaging

Labels: enhancement, ready-for-agent

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Polish only; no new behavior beyond clearer mode switching.

**State**: ready-for-agent. Clear, scoped polish task.

**Codebase notes**:
- Builds on 001 + 002. The auth screen will already render Login/Sign up.
- Visual styling already lives in `AppTheme` and the `GradientScaffold`/`FilledButton` patterns; reuse those rather than introducing new components.

**Implementation notes for the agent**:
- Make current auth mode visually obvious (segmented control or equivalent).
- Add helper text/link to switch modes.
- Submit button label matches the current mode (`Log in` / `Sign up`).
- Keep prototype/no-backend disclaimer subtle.
- Verify spacing/touch targets at iPhone simulator size.

## What to build

Polish the mock auth screen so users can clearly understand and switch between Login and Sign up. Keep prototype messaging subtle: the flow should feel real enough for the usability test while still making clear that no backend account is created.

## Acceptance criteria

- [ ] Users can switch from Login to Sign up with clear helper text or a visible toggle.
- [ ] Users can switch from Sign up back to Login with clear helper text or a visible toggle.
- [ ] Current mode is visually obvious.
- [ ] Submit button label matches the current mode.
- [ ] A subtle prototype/no-backend message is present and does not distract from the form.
- [ ] Form spacing and touch targets are comfortable on iPhone simulator size.
- [ ] Existing sign out still returns to the auth screen.
- [ ] Widget test or existing flow test confirms mode switching works.

## Blocked by

- docs/issues/001-login-with-mock-credentials.md
- docs/issues/002-sign-up-with-mock-account-details.md
