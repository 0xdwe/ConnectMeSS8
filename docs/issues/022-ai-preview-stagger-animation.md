# AI preview stagger animation (signature motion)

Labels: enhancement, ready-for-agent, polish

> *This was generated by AI during triage.*

## AI Triage Brief

**Category**: enhancement. Adds the AI preview card stagger-fade-in animation per `DESIGN.md` — when the AI preview screen appears, parsed cards fade in 240ms each at 80ms offset.

**State**: ready-for-agent. Spec pinned in `DESIGN.md → Components → AI preview` and `DESIGN.md → Motion`.

**Codebase notes**:
- AI preview screen introduced in issue 018. Cards rendered as a `ListView` of `CrmInteraction` previews.
- `MediaQuery.of(context).disableAnimations` reads OS-level reduce motion.

**Implementation notes for the agent**:
- For each preview card, wrap in an `AnimatedBuilder` with an `AnimationController(duration: 240ms, curve: Curves.easeOutQuart)`. Stagger by index: `delay = 80ms * index`. Use a `Future.delayed` on `initState` or `Stagger` widget pattern.
- Animation: opacity 0 → 1, plus a small `transform: translate(0, 8px → 0)`. Pure transform, never animate `top` or `height`.
- **Reduced motion**: when `MediaQuery.of(context).disableAnimations == true`, all cards render with opacity 1, no transform, no delay. Verify in test.
- Tests:
  - Pump preview screen with 3 cards. Pump 0ms — cards have low opacity. Pump 320ms — first three cards opaque.
  - With `disableAnimations: true`, all cards render at full opacity from frame 0.

## What to build

The other signature motion moment: a calm reveal of AI-suggested interactions, communicating "AI is showing you what it found" without flooding the screen.

## Acceptance criteria

- [ ] AI preview cards fade in (opacity 0→1) with a small upward translate (8px→0) over 240ms.
- [ ] Cards stagger by 80ms per index.
- [ ] `MediaQuery.disableAnimations == true` skips the animation; all cards render at full opacity from frame 0.
- [ ] No layout-property animation (translate via `Transform.translate`, not margin).
- [ ] All issue-018 tests pass. New tests cover both motion-on and motion-off cases.

## Blocked by

- 018 (preview screen exists with parsed-card rendering).
