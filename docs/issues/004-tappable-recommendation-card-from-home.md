## Parent

`docs/prd/2026-05-15-tappable-recommendation-cards-prd.md`

Labels: enhancement, ready-for-agent

> *This was generated by AI during triage.*

## What to build

Make every recommendation card on the Home tab tappable. Tapping a card opens that contact's Personal Connection Dashboard, where the AI Insight section already explains why the relationship was surfaced. Add a trailing chevron on the card so participants understand it is interactive.

## Acceptance criteria

- [ ] Recommendation cards on the Home tab are tappable.
- [ ] Tapping a Home recommendation card opens the contact's Personal Connection Dashboard.
- [ ] Each recommendation card shows a trailing chevron (or equivalent interactive cue).
- [ ] Card content (reason, AI insight quote, score, priority chip, category chip) is unchanged.
- [ ] Existing widget tests still pass.
- [ ] New widget test covers tapping a Home recommendation and arriving at the dashboard.

## AI Triage Brief

**Category**: enhancement. Adds an interaction; no domain changes.

**State**: ready-for-agent. Tight scope, clear acceptance criteria.

**Codebase notes**:
- The card module is `RecommendationCard` in `lib/src/widgets/crm_widgets.dart`. It is reused by `HomeTab` and `RecommendationsScreen`.
- Routing already supports `/contact/:id`; profile screen is `ContactProfileScreen` (`lib/src/features/contact_profile_screen.dart`) and contains `Recommended Action!` and `AI Insight` sections.
- Home tab is `lib/src/features/tabs/home_tab.dart`.

**Implementation notes for the agent**:
- Add an optional `VoidCallback? onTap` to `RecommendationCard`.
- Wrap the card body in `InkWell` (or equivalent) when `onTap` is provided.
- Add a trailing chevron icon column.
- In `HomeTab`, pass `onTap: () => context.push('/contact/${recommendation.contactId}')`.
- Do not change the Recommendations screen in this slice (covered by the next issue).

## Blocked by

None - can start immediately
