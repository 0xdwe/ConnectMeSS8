## Parent

`docs/prd/2026-05-15-update-with-ai-single-flow-prd.md`

Labels: enhancement, ready-for-agent

> *This was generated by AI during triage.*

## What to build

On the Personal Connection Dashboard, replace the existing AI Update tooltip-only icon with a clearly labeled primary "Update with AI" button. The button is the dashboard's primary update action and routes to the existing `/ai-update/:id` screen.

## Acceptance criteria

- [ ] Personal Connection Dashboard shows a primary, labeled "Update with AI" button.
- [ ] The Edit affordance for the contact remains accessible.
- [ ] Tapping "Update with AI" navigates to `/ai-update/:id`.
- [ ] Visual placement is consistent with the existing primary CTA pattern (`FilledButton.icon`).
- [ ] A widget test signs in, opens a contact, taps "Update with AI", and verifies the AI Update screen renders.
- [ ] All tests pass.

## AI Triage Brief

**Category**: enhancement.

**State**: ready-for-agent.

**Codebase notes**:
- Contact profile lives in `lib/src/features/contact_profile_screen.dart`.
- After issue 006 lands, the Share Activity entry points are gone; this issue replaces the lingering icon row entry for AI Update.
- The Update with AI screen is `lib/src/features/ai_update_screen.dart` and uses `TealPageHeader(title: 'AI Update')`. Update the screen header text to "Update with AI" so labels are consistent end to end.

**Implementation notes for the agent**:
- Use `FilledButton.icon` with `Icons.auto_awesome` and label "Update with AI", styled like other primary CTAs.
- Place the button near the top of the dashboard body so it is the obvious next action.
- Remove the now-unused `IconButton.filled` for AI Update from the header.
- Update the AI Update screen's header title to read "Update with AI".

## Blocked by

- docs/issues/006-remove-share-activity-flow.md
